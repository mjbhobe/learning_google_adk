# IMPORTANT

<table>
 <tr>
  <td style="background-color: #8B0000; color: #FFFFFF; padding: 15px; border-radius: 5px;">
   <strong>NOTE:</strong> This file & associated scripts have been auto-generated by Google AntiGravity IDE after scanning completed project. I am saving the generated output <em>as-is</em>. Have <strong>NOT</strong> tested the deployment yet! <br/> <strong>Use at your own risk!</strong>
  </td>
 </tr>
</table>

# Cloud Run Deployment Overview

Google Cloud Run is the perfect platform for this application because the project consists of stateless web servers (4 FastAPI agent servers and 1 Streamlit server). Cloud Run supports standard Dockerized applications and scales to zero when there's no traffic.

## Architectural Changes Just Applied 
I proactively modified the codebase logic to cleanly support dynamic URLs (Cloud Run links are dynamic, unlike `localhost`).

1. **Python Script Changes**: 
   - Instead of hardcoding URLs (e.g. `FLIGHT_URL = "http://localhost:8001/run"`), [agents/host_agent/task_manager.py](file:///home/mjbhobe/code/git-projects/learning_google_adk/projects/travel_planner/agents/host_agent/task_manager.py) now retrieves the URLs using `os.getenv("FLIGHT_URL", "http://localhost:8001/run")`. 
   - Updated [streamlit_app.py](file:///home/mjbhobe/code/git-projects/learning_google_adk/projects/travel_planner/streamlit_app.py) similarly so it reads the dynamic `HOST_AGENT_URL`.
2. **Unified [Dockerfile](file:///home/mjbhobe/code/git-projects/learning_google_adk/projects/travel_planner/Dockerfile)**: 
   - You only need **one Docker image** since every agent needs the exact same python dependencies and files. 
   - Created `/app/Dockerfile` in the root.
3. **Smart [start.sh](file:///home/mjbhobe/code/git-projects/learning_google_adk/projects/travel_planner/start.sh) Router**:
   - [start.sh](file:///home/mjbhobe/code/git-projects/learning_google_adk/projects/travel_planner/start.sh) evaluates a `SERVICE_TYPE` environment variable. This allows running 5 unique Microservices via a singular Cloud Run Container Image!
4. **[deploy.sh](file:///home/mjbhobe/code/git-projects/learning_google_adk/projects/travel_planner/deploy.sh)**: Orcehstrates all deployments. First it pushes the single Docker image. Then it calls `gcloud run deploy` 5 distinct times linking it dynamically using Environment arguments matching each agent.

## Steps to Deploy!

First ensure you have the `gcloud CLI` initialized and you are authenticated (`gcloud auth login`).

1. `cd /home/mjbhobe/code/git-projects/learning_google_adk/projects/travel_planner`
2. Make sure execution permissions are granted to the newly generated scripts:
   ```bash
   chmod +x start.sh deploy.sh
   ```
3. Establish your OpenAI API key for generation within the Cloud Run containers:
   ```bash
   export OPENAI_API_KEY="sk-something-private"
   ```
4. Perform the massive Cloud build execution!
   ```bash
   ./deploy.sh
   ```

Wait approx 5-10 minutes. At termination, it will gracefully output the newly configured `Streamlit` URL accessible across the web!

## 🗑️ Cleanup Instructions

To avoid incurring unwanted charges after testing the deployment, you can completely destroy all Cloud Run services and the Artifact Registry image repository using the included teardown script.

Make sure it's executable:
```bash
chmod +x teardown.sh
```

Execute it:
```bash
./teardown.sh
```
